{
  "devDependencies": {
    "vitest": "^0.0.0"
  },
  "scripts": {
    "test": "vitest"
  }
}

import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('should work', () => {
    expect(1 + 1).toBe(2);
  });
});

export default {
  test: {
    environment: 'jsdom'
  }
};